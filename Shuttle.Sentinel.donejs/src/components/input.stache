{{#switch type}}
	{{#case "checkbox"}}
		<input type="{{type}}" {($checked)}="checked"/>
	{{/case}}
	{{#default}}
		<input type="{{type}}" {($value)}="value" class="form-control"/>
	{{/case}}
{{/switch}}
