<cs-select label="subscription:data-store" {options}="dataStores" {(value)}="dataStoreId" />
<div class="{{#unless dataStoreId}}hidden{{/if}}">
  {{#if subscriptions.isResolved}}
  <cs-table {columns}="columns" {rows}="subscriptions.value" />
  {{else}}
  <cs-fetching title="subscription:list.title" />
  {{/if}}
</div>