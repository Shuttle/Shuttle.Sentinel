{{#switch type}}
	{{#case "checkbox"}}
		<input type="{{type}}" {($checked)}="checked"/>
	{{/case}}
	{{#default}}
		<input type="{{type}}" {($value)}="aaa" class="form-control {{elementClass}}" placeholder="{{placeholder}}"/>
		{{aaa}}
	{{/case}}
{{/switch}}
