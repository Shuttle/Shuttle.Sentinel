<sentinel-page-title title="user:list.roles" />
<sentinel-buttons>
  <sentinel-back-button />
  <sentinel-refresh-button ($click)="refresh()" />
</sentinel-buttons>
{{#if isResolved}}
<sentinel-table {columns}="columns" {rows}="roles" />
{{else}}
<sentinel-fetching title="user:list.roles" />
{{/if}}